# Zensical Alloy (YAML)

This is the same theme as the TOML example. Only the project config format
differs. Useful when migrating an existing MkDocs project to Zensical without
rewriting the config first.
