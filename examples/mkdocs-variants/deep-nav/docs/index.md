# Deep Nav Fixture

This fixture checks nested section expansion, active pills, breadcrumb behavior,
and right-side TOC density.

## Overview

Deep navigation should remain compact without hiding the current page.
