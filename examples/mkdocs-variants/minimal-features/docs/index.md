# Minimal Features Fixture

This fixture enables the theme without the larger feature list. It catches
assumptions that only work when optional navigation or code features are enabled.

```bash
zensical build
```

!!! warning "Minimal should still look intentional"

    The page should keep the same typography, callout styling, and readable code
    blocks even without optional navigation features.
