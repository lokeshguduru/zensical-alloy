# System Fonts Fixture

This fixture disables remote font configuration and relies on system stacks.

```js
const fontStack = "system";
console.log(fontStack);
```

## Reading rhythm

The page should still keep the same spacing, line height, and code alignment.
