"""Zensical Alloy theme.

The package itself is a marker. Zensical resolves the theme directory
from the package's filesystem location and reads `mkdocs_theme.yml`
to pick up the `extends: material` chain.
"""
